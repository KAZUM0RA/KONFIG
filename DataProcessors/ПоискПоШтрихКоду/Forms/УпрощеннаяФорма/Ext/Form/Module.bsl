
&НаКлиенте
Процедура ПолеВводаПоискаПриИзменении(Элемент)
	
	МассивОбъектов = ШтрихкодированиеПечатныхФормКлиент.ПолучитьСсылкуПоШтрихкодуТабличногоДокумента(СтрокаПоиска);
	Если МассивОбъектов.Количество() > 0 Тогда
		ОткрытьЗначение(МассивОбъектов[0]);
	КонецЕсли;
	
КонецПроцедуры
