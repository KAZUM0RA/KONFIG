
Процедура ПередЗаписью(Отказ)
	
	Если Ссылка = ПланыОбмена.ОбменСМобильнымиУстройствами.ЭтотУзел() И НЕ ВладелецУстройства.Пустая() Тогда
		Сообщить("Нельзя указать владельца устройства для предопределенного узла обмена.");
		Отказ = Истина;
	КонецЕсли;
	
КонецПроцедуры
