&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	ЕстьФайлыВТомах = Ложь;
	
	КоличествоФайлов = ФайловыеФункции.ПолучитьКоличествоФайловВТомах();
	Если КоличествоФайлов <> 0 Тогда
		ЕстьФайлыВТомах = Истина;
	КонецЕсли;
	
	Если НЕ ЕстьФайлыВТомах Тогда
		Предупреждение(НСтр("ru='В информационной базе отсутствуют файлы в томах.';uk=' В інформаційній базі відсутні файли в томах.'"));
		Возврат;
	КонецЕсли;

	ОткрытьФормуМодально("ОбщаяФорма.ВыборПутиКАрхивуФайловТомов");
	
КонецПроцедуры

